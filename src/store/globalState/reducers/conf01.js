import { sessionReducer as session } from 'redux-react-session';
import i18n from '../../../components/LanguageSelector/modules';
import access from '../../../routes/Access/modules';
import global from '../global';

export default {
  session,
  i18n,
  access,
  global,
};
